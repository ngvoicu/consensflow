{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bundle:ui": "node scripts/bundle-ui.mjs",
    "prepare-sidecar": "node scripts/prepare-sidecar.mjs",
    "sync-cli": "node scripts/sync-cli.mjs",
    "test:ui": "npm run bundle:ui && playwright test tests/page.spec.mjs tests/updates.spec.mjs tests/harnesses.spec.mjs",
    "build": "npm run bundle:ui && npm run prepare-sidecar && tauri build",
    "dev": "npm run bundle:ui && npm run prepare-sidecar && tauri dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@tauri-apps/cli": "^2.11.4",
    "@tauri-apps/plugin-dialog": "^2.7.3",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "esbuild": "^0.25.10"
  }
}
